This is a basic structure along with some helper functions to make it easier to write a Word plugin created by Farham Team

First, change all the SWPS prefixes to the name you want.
After changing the namespace, run the composer dumpautoload command.
